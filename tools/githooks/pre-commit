#!/bin/sh
# Formatting alone fails CI. Catch it here so a push never lights red for ktlint.
set -e
cd "$(git rev-parse --show-toplevel)"
# shellcheck disable=SC1091
. ./tools/githooks/with-java.sh
./gradlew spotlessCheck --quiet
